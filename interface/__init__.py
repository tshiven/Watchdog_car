"""Package initializer for the `interface` package.

This file makes the `interface` directory importable as a Python package
so tests and other modules can do `from interface.command_parser import ...`.
"""

# Minimal initializer — no runtime code required.
